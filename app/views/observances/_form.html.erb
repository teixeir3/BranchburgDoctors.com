<%= simple_form_for(@observance) do |f| %>
  <% if @observance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@observance.errors.count, "error") %> prohibited this observance from being saved:</h2>

      <ul>
      <% @observance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %><br>
    <%= f.date_select :start_date %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :photo_id %><br>
    <%= f.number_field :photo_id %>
  </div>
  <div class="actions">
    <%= f.submit @observance.button_text, class: "btn btn-primary" %>
  </div>
<% end %>
